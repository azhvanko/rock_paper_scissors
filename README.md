## "Rock, Paper, Scissors" with FastAPI and WebSockets 
## Dependencies
* Python 3.9
* PostgreSQL 13.7
* Docker 20.10
* Docker-compose 1.29

### Build and start the development stand. By default, the server runs on `ws://localhost:8899/`
    make run
### Teardown
    make docker_rm_all
